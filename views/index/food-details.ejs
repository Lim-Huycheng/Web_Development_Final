
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">

    <title>Menu Details</title>

    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f9f9f9;
            color: #333;
        }

        .menu-container {
            text-align: center;
            padding: 20px;
        }

        .menu-grid {
            display: flex;
            flex-direction: column;
            align-items: center;
            background: #fff;
            border-radius: 15px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            max-width: 700px;
            margin: 0 auto;
            padding: 20px;
            transition: transform 0.3s ease;
        }

        .menu-grid:hover {
            transform: translateY(-3px);
        }

        .menu-grid img {
            width: 40%;
            height: auto;
            border-radius: 30% / 30%;
            object-fit: cover;
            margin-bottom: 15px;
        }

        .menu-grid h3 {
            font-size: 1.8rem;
            margin-bottom: 10px;
            color: #000;
        }

        .menu-grid p {
            font-size: 1rem;
            color: #555;
            margin-bottom: 10px;
        }

        .menu-grid p strong {
            color: #000;
        }

        .menu-grid a {
            display: inline-block;
            text-decoration: none;
            background: #1c4427;
            color: #fff;
            padding: 10px 20px;
            border-radius: 5px;
            font-weight: bold;
            margin-top: 15px;
            transition: background 0.2s ease;
        }

        .menu-grid a:hover {
            background: #34ac62;
        }
    </style>
</head>

<body>
    <%- include('../partials/header') %>

    <div class="menu-container">
        <div class="menu-grid">
            <h3><%= food.name %></h3>
            <!-- Display the food image -->
            <img src="<%= food.image_path %>" alt="<%= food.name %>" width="200">
            <p></p>
            <p><strong>Description:</strong> <%= food.description %></p>
            <p><strong>Ingredients:</strong> <%= food.ingredients %></p> <!-- Example field -->
            <p><strong>Price:</strong> $<%= food.price %></p> <!-- Example field -->
            <a href="/exploremenu">Back to Menu</a>
        </div>
    </div>
    
    <%- include('../partials/footer') %>
    
  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>

</body>