
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Link to external CSS -->
    <link rel="stylesheet" href="css/exploremenu.css">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">

    <title>Explore Latest Menu</title>
</head>

<body>
  <%- include('../partials/header') %>

  <div class="menu-container">
    <h1>Explore Our Menu</h1>
    <p>Below are our menu</p>
    <p>Click on View Details for more information. Enjoy~~</p><br>
    <div class="menu-grid">
        <% if (food_menu && food_menu.length > 0) { %>
            <% food_menu.forEach(food => { %>
                <li>
                    <h3><%= food.name %></h3>
                    <!-- Display the food image -->
                    <img src="<%= food.image_path %>" alt="<%= food.name %>" width="200">
                    <p></p>
                    <p><%= food.description %></p>
                    <a href="/exploremenu/<%= food.id %>">View Details</a>
                </li>
            <% }) %>
        <% } else { %>
            <p>No food items available.</p>
        <% } %>
    </div>
</div>


  <%- include('../partials/footer') %>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>

</body>