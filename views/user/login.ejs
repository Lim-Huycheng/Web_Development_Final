<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <link rel="stylesheet" href="/css/login.css">  <!-- Link to your custom CSS file (optional) -->
</head>
<body>
  <%- include('../partials/header') %> <!-- Header Included -->

  <div class="login-container">
    <h2>Login</h2>

    <!-- Display flash messages -->
    <% if (messages && messages.error) { %>
      <div class="alert alert-danger">
        <%= messages.error %>
      </div>
    <% } %>

    <form action="/user/login" method="POST">
      <div class="form-group">
        <label for="username">Username</label>
        <input type="text" id="username" name="username" required>
      </div>
      
      <div class="form-group">
        <label for="password">Password</label>
        <input type="password" id="password" name="password" required>
      </div>
      
       <button type="submit"><a href="/" class="login-button">Login</a></button>
    </form>

    <p style="padding-top: 20px;">Don't have an account? <a href="/user/signup">Sign up</a></p>
  </div>

  <%- include('../partials/footer') %> <!-- Footer Included -->
</body>
</html>
