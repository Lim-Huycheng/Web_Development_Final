<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %>us</title>
    <link rel="stylesheet" href="css/index.css">
    <link rel="stylesheet" href="css/show.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</head>
<body>
    <header class="navbar navbar-expand-lg navbar-light bg-light border-bottom py-3">
        <div class="container">
            <!-- Logo -->
            <a href="/" class="navbar-brand d-flex align-items-center">
                <img src="/uploads/Divaslogo.png" width="200" height="100" class="d-block img-fluid" alt="Cooking with Node.js">
            </a>

            <!-- Navbar Toggle for Mobile Screens -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <!-- Navbar Links -->
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a href="/" class="nav-link px-2 link-dark"><i class="fa-solid fa-house"></i> Home</a>
                    </li>
                    <li class="nav-item">
                        <a href="/about" class="nav-link px-2 link-dark"><i class="fa-solid fa-address-card"></i> About us</a>
                    </li>
                    <li class="nav-item">
                        <a href="/user/login" class="nav-link px-2 link-dark"><i class="fa-solid fa-user"></i> Login</a>
                    </li>
                    <li class="nav-item">
                        <a href="/contact" class="nav-link px-2 link-dark"><i class="fa-solid fa-phone"></i> Contact us</a>
                    </li>
                </ul>

                <!-- Search Form -->
                <form method="POST" action="/search" class="d-flex">
                    <input type="search" name="searchTerm" class="form-control me-2" placeholder="Search..." aria-label="Search">
                    <button class="btn btn-outline-dark" type="submit">Search</button>
                </form>
            </div>
        </div>
    </header>

    <div class="container mt-4">
        <!-- Flash Messages -->
        <% if (messages.error) { %>
            <div class="alert alert-danger" role="alert">
                <% messages.error.forEach(error => { %>
                    <p><%= error %></p>
                <% }); %>
            </div>
        <% } %>

        <% if (messages.success) { %>
            <div class="alert alert-success" role="alert">
                <% messages.success.forEach(success => { %>
                    <p><%= success %></p>
                <% }); %>
            </div>
        <% } %>
    </div>
</body>
</html>
